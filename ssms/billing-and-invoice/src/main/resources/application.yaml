server:
  port: 8085

# URLs for downstream microservices
customer-service:
  url: http://localhost:8081
order-service:
  url: http://localhost:8084

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ssms_billing
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
