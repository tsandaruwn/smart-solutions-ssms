server:
  port: 6543

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/billing
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
